---
title: "你好，世界"
date: 2026-08-26
description: "第一篇文章，顺便测试一下各项功能"
---

这是第一篇文章。测试几样东西。

行内公式：$e^{i\pi} + 1 = 0$。

行间公式：

$$\int_0^1 x^2 \, dx = \frac{1}{3}$$

再来点代码：

```python
def hello(name: str) -> str:
    return f"你好，{name}"
```

就这样。

## 第一节

这一节用来测试图片组件和目录高亮。下面这张图应该出现在一个带圆角边框的框里，点击可以全屏放大，点图片外面或按 ESC 关闭。

{{< fig src="figures/test.png" caption="测试图片" >}}

### 一个子节

子节的内容。目录里它应该比上级条目缩进一层。滚动页面时，左侧目录的高亮会跟着当前所在章节移动，当前项左边有一条主色竖线。

再写一段，把页面撑高一点，这样滚动才有得看。静态站点生成器把 Markdown 编译成 HTML，好处是没有数据库、没有后端、部署就是把一堆文件扔到 CDN 上。

## 第二节

这一节测试表格框组件。

{{< figbox caption="上下文窗口实测" >}}
| 模型 | 宣称长度 | 实测有效长度 |
|---|---|---|
| A | 1M | 64K |
| B | 500K | 64K |
| C | 200K | 96K |
{{< /figbox >}}

再补一段正文。两端对齐只对中文生效，因为汉字等宽、没有词间空格，天然就能对齐；英文如果强行两端对齐，短行会被拉出很大的词距。

## 第三节

最后一节，用来验证滚动到底部时目录会正确高亮最后一项。

行间公式再来一个，测试 passthrough 是否正常：

$$
\begin{aligned}
a &= b + c \\
d &= e \, f
\end{aligned}
$$

结束。
